name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hola Entrenador Pokemon"
            echo "Hola Bulbasur y Pikachu 1"

      - name: Dime la fecha
        run: date
      - run: ls -l